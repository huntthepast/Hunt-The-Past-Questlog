---
import Icon from './Icon.astro';
import type { IconName } from './Icon.astro';
import { site } from '../lib/data';

const NAV: { href: string; label: string; icon: IconName }[] = [
  { href: '/games', label: 'Library', icon: 'grid' },
  { href: '/progress', label: 'Progress', icon: 'chart' },
  { href: '/achievements', label: 'Achievements', icon: 'trophy' },
  { href: '/guides', label: 'Guides', icon: 'book' },
  { href: '/wishlist', label: 'Wishlist', icon: 'bookmark' },
  { href: '/favorites', label: 'Favorites', icon: 'heart' },
];

const path = Astro.url.pathname.replace(/\/$/, '') || '/';
const isActive = (href: string) => path === href || (href !== '/' && path.startsWith(href + '/'));
---

<header class="sticky top-0 z-40 border-b border-zinc-800/80 bg-zinc-950/80 backdrop-blur" x-data="{ open: false }">
  <div class="mx-auto flex h-16 max-w-7xl items-center justify-between gap-4 px-4 sm:px-6 lg:px-8">
    <a href="/" class="group flex items-center gap-2.5" aria-label={`${site.title} home`}>
      <span class="grid size-9 place-items-center rounded-lg bg-amber-400 text-zinc-950 shadow-glow transition group-hover:bg-amber-300">
        <Icon name="gamepad" class="size-5" />
      </span>
      <span class="font-display text-2xl leading-none tracking-wide text-zinc-50">{site.title}</span>
    </a>

    <nav class="hidden items-center gap-1 md:flex" aria-label="Primary">
      {
        NAV.map((item) => (
          <a
            href={item.href}
            aria-current={isActive(item.href) ? 'page' : undefined}
            class:list={[
              'inline-flex items-center gap-1.5 rounded-lg px-3 py-2 text-sm font-medium transition',
              isActive(item.href) ? 'bg-zinc-800/80 text-amber-300' : 'text-zinc-400 hover:bg-zinc-900 hover:text-zinc-100',
            ]}
          >
            <Icon name={item.icon} class="size-4" />
            {item.label}
          </a>
        ))
      }
    </nav>

    <button
      type="button"
      class="btn md:hidden"
      @click="open = !open"
      :aria-expanded="open"
      aria-controls="mobile-nav"
      aria-label="Toggle navigation"
    >
      <Icon name="menu" class="size-5" />
    </button>
  </div>

  <nav id="mobile-nav" class="border-t border-zinc-800 bg-zinc-950 md:hidden" x-show="open" x-cloak x-transition.opacity aria-label="Primary mobile">
    <div class="mx-auto grid max-w-7xl gap-1 px-4 py-3 sm:px-6">
      {
        NAV.map((item) => (
          <a
            href={item.href}
            aria-current={isActive(item.href) ? 'page' : undefined}
            class:list={[
              'flex items-center gap-2 rounded-lg px-3 py-2 text-sm font-medium',
              isActive(item.href) ? 'bg-zinc-800/80 text-amber-300' : 'text-zinc-300 hover:bg-zinc-900',
            ]}
          >
            <Icon name={item.icon} class="size-4" />
            {item.label}
          </a>
        ))
      }
    </div>
  </nav>
</header>
